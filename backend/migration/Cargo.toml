[package]
name = "migration"
version.workspace = true
edition.workspace = true
publish.workspace = true

[lib]
name = "migration"
path = "src/lib.rs"

[dependencies]
# SeaORM
sea-orm-migration = { version = "2.0.2", features = ["runtime-tokio-rustls"] }
